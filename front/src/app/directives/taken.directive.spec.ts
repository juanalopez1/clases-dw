import { TakenDirective } from './taken.directive';

describe('TakenDirective', () => {
  it('should create an instance', () => {
    const directive = new TakenDirective();
    expect(directive).toBeTruthy();
  });
});
